# Session 2026-09-01T1408+02

**Session timestamp:** 2026-09-01T14:08+02:00

## User input

The authentication works. I want to do a sidebar to see if we can introduce swagger. the .http files are great for an integrated test but swagger is a great way for a real human to engage the api with.

## Assistant output

Swagger UI is on the same host at `/swagger`. `products.http` stays the integrated test; this is the browser explorer.

Click Authorize and paste a client-credentials access token (scope `alpha.idp.read`) — same token the HTTP client already fetches. The UI does not hold a client secret. OpenAPI lists `/product` routes only. Restart ApplicationRoot, then open `/swagger` (linked from the wwwroot demo page).
