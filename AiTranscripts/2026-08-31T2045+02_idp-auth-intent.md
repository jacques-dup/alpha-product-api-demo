# AI Transcript — 2026-08-31 20:45 (UTC+2)

## User

before we move onto further user experience design and mapping, also make reference that my intention is too have the API authenticated using Alpha's existing Identity Provider (Self hosted Duende IdentityService) which already has its authentication policies defined. My intent is NOT to do any work on this IDP service at all and just provide the scope for implementation directly as it is standard OAuth. The discovery endpoint can be listed as https://dev.auth.alpha.org/.well-known/openid-configuration  since I won't be using any production scoped services for this build. I will be adding a client configuration for this intended service later in the build, so no need to explore any implementation details at this stage.

## Assistant

Recorded in `Documentation/product-and-technical-dossier.md` section 1.7 (Authentication intent) and a pointer in 2.11.

- API and portal authenticate against Alpha's existing Identity Provider (self-hosted Duende IdentityService) as standard OAuth.
- No work on the IDP itself.
- Dev discovery only: https://dev.auth.alpha.org/.well-known/openid-configuration
- Client configuration later in the build; no implementation detail now.
