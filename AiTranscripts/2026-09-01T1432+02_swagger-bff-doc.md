# Session 2026-09-01T1432+02

**Session timestamp:** 2026-09-01T14:32+02:00

## User input

are we able to add a bff definition to the swagger doc generation?

## Assistant output

Yes. Swagger UI at `/swagger` now has a second document, **Portal BFF** (`/swagger/bff/swagger.json`), covering `/api` CRUD. Product API stays on `/swagger/v1/swagger.json` with bearer Authorize.

For BFF Try it out: log in at `/bff/login` first (cookie on this origin), then Authorize and set `X-CSRF` to `1`. The `/bff` management endpoints are not in the spec.
