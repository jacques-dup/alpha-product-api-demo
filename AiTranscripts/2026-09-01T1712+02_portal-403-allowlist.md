# Session 2026-09-01T1712+02

**Session timestamp:** 2026-09-01T17:12+02:00

## User input

when I log in with a user that is not on the allow list I briefly see the portal without any data flash to the screen, before I'm logged out again. Implement a loading spinner to obfuscate this process. Instead of directly logging the user out, show a 403 Unauthorized page with a button to trigger the log out action.

## Assistant output

That flash was `/bff/user` succeeding (OIDC session) while `/api` returned 403. React Admin then treated 403 as logout, and its default loader waits a second before showing, so the empty shell appeared first.

`checkAuth` now waits on an allow-list probe (`GET /api/languages`) behind a spinner with no delay. A 403 stays signed in and shows **403 Unauthorized** with a **Log out** button. A 401 still goes to login. Allow-listed accounts still open products.
