# Session 2026-09-01T1421+02

**Session timestamp:** 2026-09-01T14:21+02:00

## User input

This works well, lets move on to the bff endpoints for the rest of the crud actions. We can structure it the same way as the webAPI with Endpoints/ and Routes.cs. If we can reuse the existing get endpoints as is from the webapi module that would be good - we might just need to cater for bot read or readwrite scopes. Do the bff implementation and if any technical questions arise prompt me for action.

## Assistant output

Product.Bff now exposes portal CRUD under `/api`, same `Endpoints/` + `Routes.cs` shape as Product.WebApi.

GET routes reuse the WebApi handlers (auth stays on the group: cookie + `alpha.idp.readwrite` + allow-list). POST/PUT/DELETE cover languages, markets, families, tags, products, items, assets, and the join tables. Anonymous `/api` is 401; a bearer token does not satisfy cookie routes; an unlisted IDP account is 403.

Set `Bff:ClientId` / `Bff:ClientSecret` and `Portal:AllowList` in user secrets for a real login (`/bff/login`). Empty client id skips OIDC so tests still start. Portal calls need header `X-CSRF: 1`.

One choice to confirm: GET `/api/products` returns the nested **ProductPayload** (reused from WebApi). POST/PUT `/api/products` accept a **product table row**. If you want GET to return the same row shape as PUT for React Admin, say so and we can split that.
