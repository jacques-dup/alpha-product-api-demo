# Product

C# catalog API for this exercise (`Product.sln`). One process: **Product.ApplicationRoot** plugs **Product.Bff**, **Product.WebApi**, and **Product.ProductStore**. Demo page is `wwwroot` at `/`.

IDP client ids and secrets go in env / user secrets after scaffold — not in source. See `Documentation/product-and-technical-dossier.md`.
